# Dependencies
node_modules/
# package-lock.json should be tracked for exact dependency versions
# package-lock.json

# Build outputs
dist/
dist-renderer/
src/**/*.js
src/**/*.map

# build/ - We need build directory for entitlements
*.dmg
*.zip
*.blockmap

# Native binaries (built from native/ source)
src/native-tools/keyboard-simulator
src/native-tools/window-detector
src/native-tools/text-field-detector
src/native-tools/WindowDetector.dll

# Windows native tools (built from native-win/ source)
src/native-tools/window-detector.exe
src/native-tools/window-detector.dll
src/native-tools/window-detector.runtimeconfig.json
src/native-tools/window-detector.deps.json
src/native-tools/keyboard-simulator.exe
src/native-tools/keyboard-simulator.dll
src/native-tools/keyboard-simulator.runtimeconfig.json
src/native-tools/keyboard-simulator.deps.json
src/native-tools/text-field-detector.exe
src/native-tools/text-field-detector.dll
src/native-tools/text-field-detector.runtimeconfig.json
src/native-tools/text-field-detector.deps.json

# Windows C# build artifacts
native-win/*/bin/
native-win/*/obj/
native-win/bin/
native-win/obj/
native-win/nul

# Local configuration
.claude/
.junie/

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS Files
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo

# Test coverage
coverage/
.nyc_output/

# Environment files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Temporary files
tmp/
temp/
*.tmp

# User data (for development)
~/.prompt-line/

# Benchmark test data and reports
benchmarks/data/*.jsonl
benchmarks/reports/*.json
benchmarks/reports/*.txt